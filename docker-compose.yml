version: '3.8'

services:
  rapporte-app:
    build: .
    ports:
      - "8085:8085"
    volumes:
      - ./data:/app/data
    environment:
      - DATABASE_PATH=/app/data/rapporte.db
    restart: unless-stopped
